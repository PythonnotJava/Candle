// wild_utils.candle — wildcard export 模块
// 导出全部，但排除 internalSecret
export * except { internalSecret };

int square(int x) {
    return x * x;
}

int cube(int x) {
    return x * x * x;
}

int internalSecret(int x) {
    return x + 999;
}
