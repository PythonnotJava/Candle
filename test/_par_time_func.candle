load std.time alias time;
int work() { return 42; }
int main() {
    parallel {
        section { alias x = work(); print("a"); }
        section { alias y = work(); print("b"); }
    }
    print("done");
    return 0;
}
