int main() {
    // Try to access Object directly via the V_CLASS dispatch
    // Since Object is supposed to be in g_class_env, 
    // "Object" as an identifier should work
    
    // Or just check if it exists via the constructor call
    // Actually let me just test what happens
    print("DONE");
    return 0;
}
