/// http — Candle HTTP 客户端模块（std.http）
///
/// 使用原生 socket 实现（Windows Winsock / POSIX socket）。
/// 仅支持 HTTP（不支持 HTTPS）。
/// 使用: load std.http alias http;
///
/// get(url) → string    HTTP GET，返回 body 字符串；失败返回 ""

class Http {
    string get(string url);     // GET 请求，返回响应体
}
